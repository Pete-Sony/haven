export const savedPlanContext = (userId: string) => `saved-plan:${userId}`;
export const supportProfileContext = (userId: string) =>
  `support-profile:${userId}`;
export const trustedContactContext = (userId: string) =>
  `trusted-contact:${userId}`;
