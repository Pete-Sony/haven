# Optional. Without this key, Haven Relay uses its reviewed deterministic fallback.
GEMINI_API_KEY=

# Stable, structured-output capable default. Override after verifying account access.
GEMINI_MODEL=gemini-2.5-flash
